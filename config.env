# ACS CDK Infrastructure Configuration Template
# Copy this file to .env.local and update with your actual values

# Environment selection (dev/prod)
ENVIRONMENT=dev

# Existing Cognito User Pool Configuration (if you have existing Cognito resources)
# If you don't have existing Cognito resources, leave these empty and new ones will be created
EXISTING_USER_POOL_ID=us-west-1_xxxxxxxxx
EXISTING_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
EXISTING_USER_POOL_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxx

# AWS Configuration (optional - will use AWS CLI defaults if not set)
# AWS_DEFAULT_REGION=us-west-1
# AWS_PROFILE=default

# Development Notes:
# - For dev environment: resources will be created in us-west-1
# - For prod environment: resources will be created in us-east-2
# - If you have existing resources from a previous deployment, set the Cognito values above
# - If you don't have existing Cognito resources, leave them empty for new creation 